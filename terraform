terraform {
  required_providers {
    local = {
      source  = "hashicorp/local"
      version = "2.0.0"
    }
    tls = {
      source  = "hashicorp/tls"
      version = "3.0.0"
    }
  }
}

provider "local" {
  # Configuration for the local provider, if required
}

provider "tls" {
  # Configuration for the tls provider, if required
}

resource "tls_private_key" "private_ssh_key" {
  algorithm = "RSA"
  rsa_bits  = 4096
}

resource "tls_public_key" "public_ssh_key" {
  algorithm     = "RSA"
